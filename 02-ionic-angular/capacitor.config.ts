import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.luizfranzon.ionic-angular',
  appName: '02-ionic-angular',
  webDir: 'www',
};

export default config;
